apiVersion: v1
kind: Service
metadata:
  name: slugapp
  namespace: slugapi-ns
  labels:
    app: slugapp
spec:
  type: NodePort
  selector:
    app: slugapp
  ports:
  - name: http
    port: 80
    targetPort: 8000
    nodePort: 30080
  - name: metrics
    port: 8000
    targetPort: 8000
